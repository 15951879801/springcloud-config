server:
  port: 3344
spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          url: https://github.com/lanze177/springcloud-config #Github上面的git仓库名字
          #搜索目录
          search-paths:
            - springcloud-config
      label: master #读取分之
eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka
